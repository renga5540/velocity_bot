name: Velocity Bot
on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - run: pip install -r requirements.txt
      - run: python bot.py
        env:
          TOKEN: ${{ secrets.8781392368:AAEQ3K8axPmx1iZMXYWv-a_WhY3UjMIGr9M
 }}
          CHAT_ID: ${{ secrets.1482959961 }}
